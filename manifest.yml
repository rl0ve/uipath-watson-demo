---
declared-services:
  my-watson-assistant-service:
    label: conversation
    plan: free
applications:
- name: uipath-watson-demo
  command: npm start
  path: .
  memory: 256M
  instances: 1
  services:
  - UiPath-demo
  env:
    NPM_CONFIG_PRODUCTION: false
